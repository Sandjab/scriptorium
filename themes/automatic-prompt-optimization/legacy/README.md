# APO — pipeline hand-built (gelé)

Édition historique de l'APO, construite par enrichissement chirurgical à ancres
(`build/assemble.py`) d'un document de base hand-written (`apo-base.html`).

**Gelé** : non régénéré, non maintenu. `build/extract.py` et `build/assemble.py`
contiennent des chemins absolus figés (`/Users/.../napo`) et ne sont pas réexécutables
tels quels. Conservé comme golden de référence pour la charte graphique distillée dans
`.claude/skills/triptych/template/`.

Les 3 éditions livrables correspondantes sont dans `../dist/`.
