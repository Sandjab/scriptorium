[
  { "term": "Approximate Nearest Neighbor search (ANN)",
    "definition": "Recherche approchée de plus proches voisins : retrouver, dans un grand ensemble de vecteurs de haute dimension, les points les plus proches d'une requête — non plus exactement mais de façon approchée. La recherche exacte coûte O(N·d) par requête (à l'échelle d'un million de vecteurs de 1 536 dimensions, de l'ordre du milliard d'opérations flottantes) ; l'approximation troque un peu de recall contre des ordres de grandeur de latence et de mémoire en moins.",
    "see_also": "Recall@k, Locality-sensitive hashing (LSH)" },
  { "term": "Recall@k",
    "definition": "Rappel à k : métrique standard des systèmes ANN, recall@k(q) = |ANN_k(q) ∩ GT_k(q)| / k, soit la fraction des vrais top-k voisins (établis par brute-force search) effectivement retrouvés parmi les k résultats approchés. On la trace contre le débit (QPS) pour situer un algorithme sur le front de Pareto recall/vitesse.",
    "see_also": "Approximate Nearest Neighbor search (ANN)" },
  { "term": "Inverted File index (IVF)",
    "definition": "Fichier inversé : index qui partitionne la collection en nlist cellules de Voronoi via k-means ; à la requête, seules les nprobe cellules les plus proches du vecteur requête sont fouillées. La réduction de calcul est, en première approximation, proportionnelle à nprobe/nlist — fraction que la documentation FAISS qualifie de « sous-estimée » à cause des listes inverses de longueurs inégales. Héritier de l'index inversé du text retrieval transposé à la recherche visuelle par Sivic & Zisserman (2003).",
    "see_also": "Product Quantization (PQ)" },
  { "term": "Product Quantization (PQ)",
    "definition": "Quantification produit : compresse un vecteur en découpant ses D dimensions en m sous-vecteurs, chacun quantifié sur un codebook de k* centroïdes ; le code occupe m·log₂(k*) bits. Un vecteur SIFT-128 en float32 (512 octets) avec m = 8 et k* = 256 tient ainsi sur 8 octets (compression ×64). En IVF-PQ, la quantification s'applique au résidu r = y − c₁(y) plutôt qu'au vecteur brut ; l'ADC (asymmetric distance computation) garde la requête à pleine précision et reste l'estimateur le plus précis.",
    "see_also": "Inverted File index (IVF), RaBitQ" },
  { "term": "HNSW (Hierarchical Navigable Small World)",
    "definition": "Petits mondes navigables hiérarchiques : index en graphe multi-couches où la couche maximale d'un nœud est tirée selon une loi géométrique l = ⌊−ln(U)·mL⌋ (densité attendue N·M^(−l), exponentiellement décroissante vers le haut). La recherche fait une greedy search (descente gloutonne) du sommet clairsemé vers la base dense. Compétitif sur le front recall/débit en CPU non filtré avec mémoire disponible ; sa complexité O(log n) est heuristique, sans preuve formelle, et peut dégénérer en O(n) sur entrées adversariales.",
    "see_also": "Approximate Nearest Neighbor search (ANN)" },
  { "term": "Locality-sensitive hashing (LSH)",
    "definition": "Hachage localement sensible : famille de fonctions (r, cr, p₁, p₂)-sensitive telle que deux points proches (distance ≤ r) collisionnent avec probabilité ≥ p₁ et deux points lointains (distance ≥ cr) avec probabilité ≤ p₂, p₁ > p₂. L'exposant ρ = log(1/p₁)/log(1/p₂) borne le temps de requête en O(n^ρ) et l'espace en O(n^(1+ρ)) ; ρ < 1 est la condition du sous-linéaire. LSH ne retourne qu'un voisin c-approximatif (pas le voisin exact) et admet des faux négatifs.",
    "see_also": "Approximate Nearest Neighbor search (ANN)" },
  { "term": "Filtered ANN",
    "definition": "Recherche filtrée : croiser similarité vectorielle et contraintes de métadonnées (prédicats). La littérature distingue grossièrement trois moments d'application — pré-filtrage, post-filtrage, et filtrage intégré à la traversée ou à l'index — sans terminologie unifiée. Sous faible sélectivité, le sous-graphe HNSW des vecteurs qualifiants se fragmente en composantes déconnectées et le recall s'effondre ; ACORN (expansion aux two-hop neighbors) et IVFFlat (sélection par cluster) restaurent le compromis.",
    "see_also": "HNSW (Hierarchical Navigable Small World), Inverted File index (IVF)" },
  {
    "term": "FAISS",
    "definition": "Facebook AI Similarity Search : bibliothèque open source (Meta, 2017) de recherche de similarité et de clustering sur vecteurs denses à grande échelle. Elle implémente les principales familles d'index ANN — IVF (fichier inversé), PQ (quantification de produit), HNSW — sur CPU et GPU, et sert de référence d'implémentation pour la recherche vectorielle approchée.",
    "see_also": "Inverted File index (IVF), Product Quantization (PQ), HNSW (Hierarchical Navigable Small World)"
  }
]
